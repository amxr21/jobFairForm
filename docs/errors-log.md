# Error Reference Log

Append new entries at the bottom. Never delete old entries — the whole point is the accumulated history.

| Date | Project | Stack | Symptom | Root cause | Fix | Prevention | Related errors |
|---|---|---|---|---|---|---|---|
| 2026-07-11 | JobFair Form | React 17/Vite | `Uncaught TypeError: (fieldMissing \|\| "").split is not a function` crashing the whole wizard on first render | `FormContext.jsx`'s `fieldMissing` state starts as an **array** (initial `useState`) but becomes a comma-joined **string** after the first `updateFormData` call — new validation-state code in `Input.jsx`/`SelectInput.jsx` assumed it was always a string | Normalize both shapes before use: `Array.isArray(fieldMissing) ? fieldMissing : (fieldMissing \|\| "").split(", ")` in `Input.jsx`, `SelectInput.jsx`, and `Form.jsx`'s missing-fields toast | A type/shape comment on `fieldMissing` in `FormContext.jsx`, or normalizing it to always be an array at the source, would prevent every future consumer from hitting this | — |
| 2026-07-11 | JobFair Form | React/Vite/Tailwind | `legacy-drift-check` against `ui-system-check`: native `<input type="date">` used for Date of Birth / Expected-to-Graduate fields | Form predates the shadcn-only convention; these two fields were built with plain native date inputs before the rule existed | Scoped fix: replace with shadcn `Calendar`+`Popover` (date fields only — custom Select/multi-select components left as-is per user decision, since they're not native `<select>` and rewriting them risks working search/chip/keyboard-nav behavior) | Running `ui-system-check`'s scanner at PR time would catch new native elements; it currently misses `type` set via spread props (`{...inputProps}`) rather than literal JSX — the scanner has a blind spot for dynamically-assigned `type` attributes | — |
| 2026-07-11 | JobFair Form | React/Vite/Tailwind | `legacy-drift-check`: all icons are hand-drawn inline SVGs, not sourced from shadcn.io/icons per Rule 2 | Predates the icon-sourcing convention | Not fixed in this pass (P3, cosmetic) — flagged for a future icon swap | — | — |
| 2026-07-11 | JobFair Form | CSS | `legacy-drift-check`: global `* { transition: all ease-in-out 0.3s !important; }` in `style.css` — blanket transition-all anti-pattern per `ux-animation-reviewer` | Predates the motion standard; the wizard's step/confirm animations (`classList.replace`-driven) currently depend on `all` catching `opacity`/`height`/`padding` toggles that have no explicit per-element transition classes | Not fixed — narrowing it risks breaking the working step-transition animation without a dedicated regression pass; accepted as known debt | A per-property transition list on the specific elements that animate (instead of a global blanket rule) would remove the dependency and make this safe to narrow later | — |
| 2026-07-11 | JobFair Form | Node/Express/Prisma | Form's live application submissions were potentially not landing in the same database the dashboard reads from | `form/backend/.env`'s `DATABASE_URL` pointed at a local MariaDB (`localhost:3306`) left over from local dev, instead of the shared production Aiven MySQL instance dashboard's backend uses; form's `config/prisma.js` also lacked the SSL/CA-cert handling Aiven requires | Updated `DATABASE_URL` to the same Aiven connection string as dashboard, copied dashboard's CA cert to `form/backend/certs/ca.pem` (gitignored), replaced `config/prisma.js` with dashboard's SSL-aware version | Both apps sharing one database should have shared/documented DB config, or at minimum a startup check that fails loudly if `DATABASE_URL` doesn't match the expected host | — |
| 2026-07-11 | JobFair Form | React/Vite | Chrome CORS error `No 'Access-Control-Allow-Origin' header` on the ticket-lookup request | `form/backend/app.js` set `Access-Control-Allow-Origin` to a JS **array** of origins, which serialized to a comma-joined string the browser rejects as an invalid single origin — broke every cross-origin request | Echo back `req.headers.origin` when it's in an `ALLOWED_ORIGINS` allowlist (single string), add `Vary: Origin`, short-circuit OPTIONS preflight with 204, and remove the redundant duplicate `app.use(cors())` | `Access-Control-Allow-Origin` is spec'd as a single origin (or `*`) — never an array; use the `cors` package's `origin` option or echo one matched origin | — |
| 2026-07-11 | JobFair Form | Node/Cloudinary | CV files saved to Cloudinary had no `.pdf` extension in their URL, so they downloaded without an extension | `config/cloudinary.js` stripped the extension when building `public_id` AND used `resource_type: 'raw'`, for which Cloudinary does NOT auto-append the extension (unlike images) | Bake the original extension back into the `public_id` (`${base}.${ext}`) in the CloudinaryStorage params | For `resource_type: 'raw'` uploads the extension must always be part of the `public_id` or set via the `format` param — it is never inferred | — |
| 2026-07-11 | JobFair Form | React/Vite/Tailwind | INTENTIONAL EXCEPTION (not a bug): native `<select>` used for month/year jump in the from-scratch `DatePicker.jsx`, which `ui-system-check` Rule 1 flags | shadcn's `Calendar`+`react-day-picker` broke repeatedly (v10 API mismatch, table-width blowout); user explicitly approved rebuilding the date picker from scratch with "built in tag with some restyling" | Kept 2 native `<select>` (month + year) inside the custom DatePicker — deliberate, user-sanctioned. Everything else (day grid, popover, trigger) is custom/portaled, not native | Do NOT re-flag this as accidental drift — it's a documented, approved tradeoff for a component that broke 4× under the shadcn approach | — |
| 2026-07-11 | JobFair Form | React/Vite/a11y | `a11y-check`: form field labels are `<h2>` elements, not `<label htmlFor>` associated with their inputs — screen readers don't announce the label when the field is focused | Predates a11y review; the shared `Input.jsx`/`SelectInput.jsx` render labels as visual-only `<h2>` | DEFERRED per user (P2, known debt). The keyboard-operability sibling issue (SelectInput trigger was a non-focusable `<div>`) WAS fixed — added role=button/tabIndex/keydown/focus-visible ring | Convert the `<h2>` labels to `<label htmlFor={Id}>` with matching input `id`s across Input/SelectInput; would also satisfy Lighthouse's label audit | — |
| 2026-07-23 | JobFair Form | React(Vite) on Vercel + Express/Prisma on Render (free tier) | Chrome CORS error `No 'Access-Control-Allow-Origin' header` on `POST /applicants` from job-fair-form.vercel.app → jobfair-1.onrender.com; submission always failed with `AxiosError: Network Error` | NOT a CORS/code bug. The Render service was **entirely down** — every path returned `404` + `x-render-routing: no-server` in ~0.3s (Cloudflare edge, no instance behind it). The request never reached Express, so the CORS middleware never ran and no ACAO header was attached; the browser reports that as a CORS failure, masking the real cause. Booting `app.js` locally proved the allowlist is correct (preflight → 204 with the Vercel origin echoed; unknown origin → no header). Compounding config rot: `.env`/`.env.example` point at a **Railway** URL while the code falls back to **Render**, and both hosts are 404 | Infra fix (redeploy Render + set `VITE_API_URL` in Vercel to whichever host is real) is the user's to make. Code-side: added `GET /health` so uptime checks can confirm the service is actually up without touching the DB or an auth'd route | A missing-ACAO error means the response had no CORS headers — which happens when the server is DOWN just as much as when CORS is misconfigured. Check the raw response first (`curl -i`): a fast 404 with `x-render-routing: no-server` (or Railway's `x-railway-fallback`) is a dead service, whereas a cold start hangs 30–60s. Never debug the CORS config until a non-CORS request to the same host returns something from the app itself. Keep the code's fallback URL and the `.env` URLs pointing at the SAME host | Same symptom as the 2026-07-11 CORS-array entry above but a different layer (service down, not header serialization). Same free-tier-Render class as the dashboard's cold-start entry in the general log (2026-07-22) |
| 2026-07-23 | JobFair Form | React(Vite) | Submitting a fully-completed application was silently rejected with a "Please complete…" toast; conversely, applications submitted with required fields still empty | `Form.jsx` gated submission on how many `formData` values were non-empty (`filledFields.length >= 16`) instead of checking which fields. A complete application totals exactly 16, so clearing the OPTIONAL CGPA field dropped it to 15 and blocked a valid submit; padding optional fields (LinkedIn, preferred city, interests) pushed an incomplete one to 19 and let it through. Verified both directions against the real object shape | Replaced with an explicit `REQUIRED_FIELDS` list + per-field `isFieldFilled`, validated BEFORE showing the loading overlay (a failed check used to flash it), plus a `submitPhase !== "idle"` guard so a double-click can't create two applicants | Never gate a form on a COUNT of filled fields — the threshold silently couples to how many optional fields exist and breaks the moment one is added or cleared. Enumerate required keys explicitly. A unit test asserting "complete app passes / each single omission fails" would have caught both directions immediately | — |
| 2026-07-23 | JobFair Form | Node/Express/nodemailer | Backend process could die mid-operation, taking the form offline for all users until the host restarted it | `sendEmail` (Outlook SMTP) and `QRCode.toDataURL` both run as best-effort side effects INSIDE the QR callback, AFTER `res.json()` has already been sent. Neither had a rejection handler, so a bad SMTP credential, missing recipient, or network blip became an unhandled rejection that exited the process. A QR render error also left `url` undefined while still returning 200, producing a "successful" ticket with no QR | Guarded `sendEmail` against a missing recipient, attached `.catch()` at both call sites, logged QR failures without failing the already-committed submission (`url ?? null`), and added `unhandledRejection`/`uncaughtException` handlers in `server.js`. Also fixed a double-response in `loginUser` that threw `ERR_HTTP_HEADERS_SENT` | Any async work that runs AFTER the response is sent must have its own `.catch()` — it can no longer fail the request, so it fails the process instead. Treat post-response side effects (emails, webhooks, analytics) as fire-and-forget with explicit error capture | — |
| 2026-07-23 | JobFair Form | React(Vite) + multer | "Can't exceed 2MB!" alert fired when the user CANCELLED the file picker without choosing anything; also a 3MB CV was rejected by the UI though the backend accepted it | Two bugs in `uploadCV`: (1) `file?.size <= 2*1024*1024` evaluates to `false` when `file` is `undefined` (cancelled picker), falling into the else-branch and showing the error; (2) the frontend cap (2MB) didn't match multer's `limits.fileSize` (4MB) in `config/cloudinary.js` | Early-return on `!file`, aligned the cap on 4MB via a named `MAX_CV_BYTES` constant with a comment pointing at multer's limit, and replaced the native `alert()` with the app's toast | Optional-chaining a value into a COMPARISON silently changes the branch taken when it's undefined — guard the absent case explicitly before comparing. Client and server size/type limits must reference one documented number; drifting them means the UI rejects files the API would accept (or vice-versa) | — |
| 2026-07-23 | JobFair Form | React(Vite) | `CheckId`/`QrScanner` hit the production backend even during local development, so dev testing wrote to prod data | Both components hardcoded `https://jobfair-1.onrender.com` instead of reading `import.meta.env.VITE_API_URL`, unlike `Form.jsx`/`TicketLookup.jsx`/`useLogin`/`useSignUp` which all use the env-var-with-fallback pattern | Switched both to `import.meta.env.VITE_API_URL \|\| <fallback>`, matching the established pattern | When one API base URL is introduced via env var, grep the whole `src/` for the literal host to catch stragglers — a partially-applied convention is worse than none, because dev traffic silently reaches prod. Same root as the earlier hardcoded-localhost fallback in `Form.jsx` | — |
| 2026-07-23 | JobFair Form | Node/Express | Applicant PII (full names, emails, phone numbers, university IDs) written to server logs on every request | A catch-all middleware in `app.js` logged `req.method`/`req.path`/`req.body` for all routes, so every `POST /applicants` dumped the complete application body. Additional `console.log(applicant)` calls in the controllers logged whole DB rows | Removed the body-logging middleware and the record dumps; converted genuine failures to `console.error` with a message only (no payload). Also replaced a misleading `"Request is never sent...T-T"` 500 body with an actionable message | Never log full request bodies on routes that accept personal data — log method/path/status only, and log identifiers rather than records. A blanket request logger added for debugging is the most common way PII reaches logs | — |
